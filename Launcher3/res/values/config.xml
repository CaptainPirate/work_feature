<resources>
<!-- Dynamic Grid -->
    <integer name="config_dynamic_grid_max_long_edge_cell_count">6</integer>
    <integer name="config_dynamic_grid_max_short_edge_cell_count">5</integer>
    <integer name="config_dynamic_grid_min_edge_cell_count">3</integer>
    <!-- Out of 100, the percent of space the overview bar should try and take vertically. -->
    <integer name="config_dynamic_grid_overview_icon_zone_percentage">16</integer>
    <!-- Out of 100, the percent to shrink the workspace during overview mode. -->
    <integer name="config_dynamic_grid_overview_scale_percentage">100</integer>

<!-- Miscellaneous -->
    <bool name="config_largeHeap">false</bool>
    <bool name="is_tablet">false</bool>
    <bool name="is_large_tablet">false</bool>
    <bool name="allow_rotation">false</bool>

    <!-- Max number of page indicators to show -->
    <integer name="config_maxNumberOfPageIndicatorsToShow">21</integer>

    <!-- App data backup and restore. To enble backup, register with an android backup service.
         http://developer.android.com/guide/topics/data/backup.html#BackupKey -->
    <bool name="enable_backup">false</bool>

<!-- DragController -->
    <integer name="config_flingToDeleteMinVelocity">-1500</integer>

<!-- AllApps/Customize/AppsCustomize -->
    <!-- The alpha of the AppsCustomize bg in spring loaded mode -->
    <integer name="config_workspaceScrimAlpha">55</integer>
    <integer name="config_workspaceUnshrinkTime">300</integer>
    <integer name="config_overviewTransitionTime">250</integer>

    <!-- Out of 100, the percent to shrink the workspace during spring loaded mode. -->
    <integer name="config_workspaceSpringLoadShrinkPercentage">94</integer>

    <!-- Fade/zoom in/out duration & scale in the AllApps transition.
         Note: This should be less than the workspaceShrinkTime as they happen together. -->
    <integer name="config_appsCustomizeRevealTime">220</integer>
    <integer name="config_appsCustomizeZoomInTime">350</integer>
    <integer name="config_appsCustomizeZoomOutTime">600</integer>
    <integer name="config_appsCustomizeZoomScaleFactor">7</integer>
    <integer name="config_appsCustomizeFadeInTime">250</integer>
    <integer name="config_appsCustomizeFadeOutTime">200</integer>
    <integer name="config_appsCustomizeWorkspaceShrinkTime">300</integer>

    <integer name="config_appsCustomizeConcealTime">250</integer>
    <integer name="config_appsCustomizeItemsAlphaStagger">60</integer>

    <!-- This constant stores the ratio of the all apps button drawable which
         is used for internal (baked-in) padding -->
    <integer name="config_allAppsButtonPaddingPercent">17</integer>

    <integer name="config_workspaceDefaultScreen">1</integer>

    <!-- Tab transition animation duration -->
    <integer name="config_tabTransitionDuration">250</integer>

    <!-- The slope, in percent, of the drag movement needed to drag an item out of
         AppsCustomize (y / x * 100%)  -->
    <integer name="config_appsCustomizeDragSlopeThreshold">150</integer>

<!-- Workspace -->
    <!-- Whether or not the drop targets drop down as opposed to fade in -->
    <bool name="config_useDropTargetDownTransition">false</bool>

    <!-- The transition duration for the background of the drop targets -->
    <integer name="config_dropTargetBgTransitionDuration">0</integer>

    <!-- The duration (in ms) of the fade animation on the object outlines, used when
         we are dragging objects around on the home screen. -->
    <integer name="config_dragOutlineFadeTime">900</integer>

    <!-- The alpha value at which to show the most recent drop visualization outline. -->
    <integer name="config_dragOutlineMaxAlpha">128</integer>

    <!-- Parameters controlling the animation for when an item is dropped on the home screen,
         and it animates from its old position to the new one. -->
    <integer name="config_dropAnimMinDuration">100</integer>
    <integer name="config_dropAnimMaxDuration">500</integer>

    <!-- The duration of the UserFolder opening and closing animation -->
    <integer name="config_folderExpandDuration">250</integer>
    <integer name="config_materialFolderExpandDuration">200</integer>
    <integer name="config_materialFolderExpandStagger">60</integer>

    <!-- The distance at which the animation should take the max duration -->
    <integer name="config_dropAnimMaxDist">800</integer>

    <!-- Properties controlling the workspace fade-out during dragging -->
    <integer name="config_dragFadeOutAlpha">80</integer>
    <integer name="config_dragFadeOutDuration">250</integer>

    <!-- Camera distance for the overscroll effect -->
    <integer name="config_cameraDistance">8000</integer>

<!-- Hotseat -->
    <bool name="hotseat_transpose_layout_with_orientation">true</bool>

    <!-- Memory debugging, including a memory dump icon -->
    <bool name="debug_memory_enabled">false</bool>

    <!-- Name of a subclass of com.android.launcher3.AppFilter used to
         filter the activities shown in the launcher. Can be empty. -->
    <string name="app_filter_class" translatable="false"></string>

    <!-- Name of a subclass of com.android.launcher3.BuildInfo used to
         get build information. Can be empty. -->
    <string name="build_info_class" translatable="false"></string>

    <bool name="disable_original_search_bar">true</bool>
    <bool name="folder_full_screen">true</bool>
    <!-- bug 183547 , tangzhongfeng.wt, MOD, 20160601 -->
    <integer name="folder_columns">3</integer>
    <integer name="folder_rows">4</integer>
    <integer name="folder_hit_margin">60</integer>

<!-- icon click effect -->
    <bool name="apply_icon_effect">true</bool>
    <integer name="icon_backward_scale_factor">90</integer>
    <integer name="icon_overshot_scale_factor">110</integer>
    <integer name="icon_backward_scale_time">40</integer>
    <integer name="icon_overshot_scale_time">40</integer>
    <integer name="icon_restore_scale_time">10</integer>

<!-- Sceen manage mode -->
    <integer name="sceen_manage_mode_cell_layout_background_alpha">40</integer>
    <!-- Req_launcher_01.01.14, tangzhongfeng.wt, MOD, 20160325 -->
    <integer name="user_default_page_index">0</integer>
    <integer name="max_pages">30</integer>

<!-- Screen Switch Effects -->
    <integer name="enter_effect_preview_anim_duration">500</integer>
    <integer name="scale_compensation_in_overview">90</integer>

<!-- Frequent Used Apps -->
    <integer name="frequent_used_apps_grid_column">4</integer>
    <integer name="frequent_used_apps_max_item_count">16</integer>

<!-- FolderIcon -->
    <integer name="folder_icon_preview_NXN">3</integer>

<!-- Unread Info -->
    <integer name="max_unread_count">999</integer>
    <string name="exceed_max_unread_count_text">"999+"</string>

<!-- Blur Effect -->
    <integer name="background_blur_radius">12</integer>

<!-- Customzie App List -->
    <string name="app_list_customizer" translatable="false">com.android.launcher3.CmccAppListCustomizer</string>
    <bool name="uncustomized_app_sort_by_installed_time">true</bool>

<!-- Expand Status Bar -->
    <integer name="threshold_for_expand_statusbar">300</integer>

<!-- Third Party App Unread Badge -->
    <bool name="support_third_party_unread_badge">false</bool>

<!-- Hotseat -->
    <bool name="disable_hotseat_text">false</bool>

    <integer name="launcher_text_size_in_sp">12</integer>
    
    <integer name="tempedit_cell_count_x">6</integer>
    <integer name="tempedit_cell_count_y">1</integer>
    <integer name="tempedit_max_page">99</integer><!-- bug 204480, liyichong.wt, MODIFY 20160816 -->

    <integer name="config_mAddScreenAnimationTime">750</integer>
</resources>
